import { Story, Meta } from '@storybook/react/types-6-0';
import ExperienceBar from './index';

export default {
  title: 'ExperienceBar',
  component: ExperienceBar,
} as Meta;

export const Basic: Story = () => <ExperienceBar />;
